import React from 'react'
import { StyleSheet, View } from 'react-native'

export default function Profile() {
  return (
    <View>
      {/* <FlatList
        data={FRUITS}
        renderItem={({item}) => <PersonneItem  {...item}/>}
        keyExtractor={(item) => item}
      /> */}
    </View>
  )
}

const styles = StyleSheet.create({})